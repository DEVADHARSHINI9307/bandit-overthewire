Level 12 → Level 13
ssh bandit12@bandit.labs.overthewire.org -p 2220

xxd -r data.txt > file   //converts hex dump back into binary file

gunzip / bunzip2 / tar    //repeatedly decompresses the file until plain text is obtained

password of next level is displayed
