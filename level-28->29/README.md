Level 28 → Level 29
// username: bandit28
ssh bandit28@bandit.labs.overthewire.org -p 2220

// enter password for level 28

git clone ssh://bandit28-git@localhost/home/bandit28-git/repo
cd repo

//clones the repository and moves into it

git log        //shows commit history

git show           //displays previous commit where password is visible

exit

password of next level (bandit29) is displayed
